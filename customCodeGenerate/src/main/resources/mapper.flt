package ${packageName};

import com.shucong.mall.entity.${domainName};
import java.util.List;
import org.apache.ibatis.annotations.*;

@Mapper
public interface ${domainName}Mapper {
    int insert(${domainName} ${domainName?uncap_first});

    int update(${domainName} ${domainName?uncap_first});

    ${domainName} findById(int id);

    List<${domainName}> findAll();

    int deleteById(int id);
}